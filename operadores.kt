
fun main() {
    var b:Boolean = false
    b = !(b)
    println(b)
    println((4+2)*2)
}

/**
 * OPERADORES LOGICOS
 * && -> E
 * || -> OU
 * ! -> NEGAÇÃO
 *
 * OPERADORES MATEMATICOS
 * + -> ADIÇÃO
 * - -> SUBTRAÇÃO
 * * -> MULTIPLICAÇÃO
 * / -> DIVISAO
 * % -> RESTO DA DIVISAO
 *
 * OPERADORES COMPARATIVOS
 *  > MAIOR QUE
 *  >= MAIOR OU IGUAL
 *  < MENOR QUE
 *  <= MENOR OU IGUAL
 *  == IGUALDADE
 *  = ATRIBUIÇÃO
 *  != DIFERENTE
 *
 */
